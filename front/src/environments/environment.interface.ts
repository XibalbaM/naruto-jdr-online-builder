/**
 * This is the interface for the environment configurations.
 * @class Environment
 */
export default class Environment {
  api_url!: string;
  contactUrl!: string;
  recaptchaSiteKey!: string;
  discordLogUrl!: string;
}
