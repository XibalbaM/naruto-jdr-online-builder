export default class Environment {
    env!: string;
    token!: string;
    clientId!: string;
    clientSecret!: string;
    devGuildId!: string;
    api_url!: string;
    jwt_secret!: string;
    jwt_expiration!: number;
}